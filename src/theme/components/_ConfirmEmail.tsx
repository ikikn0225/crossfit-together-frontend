import styled, { css } from 'styled-components';
import { _SubContainer } from './_Layout';

export const _ConfirmEmailContainer = styled(_SubContainer)`
    width:auto;
    padding:0;
`;

export const _ConfirmEmailSpan = styled.span`
    display:block;
    text-align:center;
    font-size:1.5em;
    margin-bottom:20px;
`;